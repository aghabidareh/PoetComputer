{
  "output_dir": "./models/poetry",
  "num_train_epochs": 3,
  "batch_size": 8,
  "learning_rate": 5e-5,
  "evaluation_steps": 500,
  "save_steps": 500,
  "warmup_steps": 500,
  "logging_dir": "./logs",
  "gradient_accumulation_steps": 4,
  "fp16": true,
  "early_stopping_patience": 2
}